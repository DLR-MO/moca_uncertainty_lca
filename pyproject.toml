[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uncertainty_lca"
version = "0.1.0"
description = "Uncertainty LCA tools"
requires-python = ">=3.10"